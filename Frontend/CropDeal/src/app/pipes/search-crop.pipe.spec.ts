import { SearchCropPipe } from './search-crop.pipe';

describe('SearchCropPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCropPipe();
    expect(pipe).toBeTruthy();
  });
});
